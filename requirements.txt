# Exact versions used by the published reproduction check. XGBoost minor versions
# can produce different trees and fitted blend weights from the same seed.
numpy==2.0.2
pandas==2.3.3
pyarrow==21.0.0
scipy==1.13.1
scikit-learn==1.6.1
xgboost==3.2.0
